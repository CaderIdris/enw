{%- set default_dict = {} -%}
{%- for k, v in used_keys.items() -%}
{%- set _ = default_dict.update( {k: ',' if v else '' } ) -%} 
{%- endfor -%}
Output Requirements - Sets of Particle/Puff Information:
Output Name{{ ',Particles?' if used_keys['particles'] }}{{ ',Puffs?' if used_keys['puffs'] }}{{ ',First Particle' if used_keys['first_particle'] }}{{ ',Last Particle' if used_keys['last_particle'] }}{{ ',First Puff' if used_keys['first_puff'] }}{{ ',Last Puff' if used_keys['last_puff'] }}{{ ',Source' if used_keys['source'] }}{{ ',Met?' if used_keys['met'] }}{{ ',Mass?' if used_keys['mass'] }}{{ ',Plume Rise?' if used_keys['plume_rise'] }}{{ ',Dispersion Scheme?' if used_keys['dispersion_scheme'] }}{{ ',Puff Family?' if used_keys['puff_family'] }}{{ ',Fate Info?' if used_keys['fate_info'] }}{{ ',H-Coord' if used_keys['h_coord'] }}{{ ',Z-Coord' if used_keys['z_coord'] }}{{ ',T-Grid' if used_keys['t_grid'] }}{{ ',Sync?' if used_keys['sync'] }}{{ ',Output Format' if used_keys['output_format'] }}{{ ',Output Route' if used_keys['output_route'] }}
{%- for row in rows %}
{{ row['name'] }}{{ ',' ~ row['particles'] if row['particles'] is not none else default_dict['particles'] }}{{ ',' ~ row['puffs'] if row['puffs'] is not none else default_dict['puffs'] }}{{ ',' ~ row['first_particle'] if row['first_particle'] is not none else default_dict['first_particle'] }}{{ ',' ~ row['last_particle'] if row['last_particle'] is not none else default_dict['last_particle'] }}{{ ',' ~ row['first_puff'] if row['first_puff'] is not none else default_dict['first_puff'] }}{{ ',' ~ row['last_puff'] if row['last_puff'] is not none else default_dict['last_puff'] }}{{ ',' ~ row['source'] if row['source'] is not none else default_dict['source'] }}{{ ',' ~ row['met'] if row['met'] is not none else default_dict['met'] }}{{ ',' ~ row['mass'] if row['mass'] is not none else default_dict['mass'] }}{{ ',' ~ row['plume_rise'] if row['plume_rise'] is not none else default_dict['plume_rise'] }}{{ ',' ~ row['dispersion_scheme'] if row['dispersion_scheme'] is not none else default_dict['dispersion_scheme'] }}{{ ',' ~ row['puff_family'] if row['puff_family'] is not none else default_dict['puff_family'] }}{{ ',' ~ row['fate_info'] if row['fate_info'] is not none else default_dict['fate_info'] }}{{ ',' ~ row['h_coord'] if row['h_coord'] is not none else default_dict['h_coord'] }}{{ ',' ~ row['z_coord'] if row['z_coord'] is not none else default_dict['z_coord'] }}{{ ',' ~ row['t_grid'] if row['t_grid'] is not none else default_dict['t_grid'] }}{{ ',' ~ row['sync'] if row['sync'] is not none else default_dict['sync'] }}{{ ',' ~ row['output_format'] if row['output_format'] is not none else default_dict['output_format'] }}{{ ',' ~ row['output_route'] if row['output_route'] is not none else default_dict['output_route'] }}
{%- endfor %}
